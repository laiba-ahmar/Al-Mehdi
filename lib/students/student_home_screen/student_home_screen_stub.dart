import 'package:flutter/material.dart';

class StudentHomeSreenWeb extends StatelessWidget {
  const StudentHomeSreenWeb({super.key});
  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}